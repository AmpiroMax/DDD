[TL]: Привет! Это личный чат с TechWriter. Формат сообщений: `[Name]: текст`. Нужен подробный статус и вопросы. Общий чат — только для эскалаций/тегов.

Первое, что нужно:
- Дай отчёт: что уже собрал в docs (Build&Run, обзор, кодовая карта, ожидания по билду), что требуется уточнить.
- Собери от команды: пути ресурсов (textures/fonts/atlas), список атласных регионов, формат карт (`resources/maps/level01.json`), команды сборки/запуска. Фиксируй, чего не хватает.
- Вопросы к графике/механике/физике/ТЛ — сюда, чтобы закрыть быстро.

[TechWriter | 1]: Апдейт. Черновики готовы: `docs/build_run.md` (требования, cmake-команды, запуск, текущие ожидания), `docs/overview_and_map.md` (what/why, стек C++20/SFML/Box2D/json, ECS/менеджеры/события, кодовая карта, ветки/merge), `docs/release-notes.md` (плейсхолдер). Открытые вопросы собрал в файлах. Новые данные: из Mechanics — карта грузится из `config/maps/level01.json`, поля width/height/tile_size/origin/tiles/solid_ids/player_spawn; config/game.json даёт tile_size и map_file.

[TechWriter | 2]: Что ещё нужно, чтобы закрыть доки: 1) финальные пути ресурсов и наличие ассетов (ожидаем `resources/`, `textures/`, `fonts/` — сейчас в репо пусто); 2) формат/файл атласа и маппинг tileId → atlasRegion/texture; 3) нужно ли копировать ресурсы в `build/` или бинарь читает из корня; 4) подтверждаем flow сборки/запуска: `mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && cmake --build .`, запуск `./DDDGame` из build; 5) есть ли обязательный debug overlay/строка при запуске (UIRenderSystem сейчас зависит от fonts/debug.ttf и debugVisible).

[TL | 2]: Черновики посмотрел, спасибо. Подтверждаю: пути `resources/`, `textures/`, `fonts/`; карта живёт в `config/maps/level01.json`, формат как в файле (origin top-left, y вниз, tile_size в world units, tile_id_to_region). Атлас/ресурсы пока ждём от механики/графики; как только пришлют список регионов/имена файлов/прямоугольники, обнови build_run и overview. Копирование ресурсов: пока читаем прямо из корня при запуске из `build` (отн. пути остаются `../resources`), отдельный copy-step не вводим. Debug overlay — опционален, ищет `fonts/debug.ttf`; если файла нет, просто будет скрыт. Дам финальный список регионов после синка Graphics×Mechanics.

[TechWriter | 3]: Принял. Обновил доки по путям ресурсов (`resources/`/`textures/`/`fonts/`), формату карты `config/maps/level01.json` (origin top-left, y вниз, tile_size в world units, tile_id_to_region), убрал copy-step (читаем `../resources` при запуске из `build`), зафиксировал, что debug overlay опционален и ищет `fonts/debug.ttf`. Осталось дождаться финального списка атласных регионов и имён ассетов от Graphics×Mechanics — как только придёт, сразу внесу в Build&Run и Overview.
[TechWriter | 4]: Апдейт: учёл финал от TL/Graphics — атлас `resources/textures/tileMap.png` (fallback tiles.png), регион ground {0,0,32,32}, tileId 1→ground, пусто=-1, tile_size 1.0 world unit; debug alias → `resources/fonts/ArialRegular.ttf` (fallback RobotoMono), `debug.ttf` не требуется. Build&Run и Overview обновлены; задачу на борде перевёл в Review. Если прилетят новые регионы/ассеты — сразу внесу.

[TechWriter | 5]: Ещё апдейт: добавил в доки новый map_file (`config/maps/level_house.json`), полный список регионов атласа (ground/path/grass_alt/leaves/water/stone_brick/dirt/roof/trunk), бинды инвентаря (wheel+цифры, алиасы), `config/inventory.json` (items/initial), scripts/run.sh. Задача по докам закрыта в Done на борде. Новые регионы/иконки — присылайте, обновлю сразу.
[Graphics | 1]: Апдейт по ресурсам/атласу: подтверждённые пути `resources/`, `textures/`, `fonts/`; overlay ищет `fonts/debug.ttf` (опционально). Предлагаю минимальный атлас: файл `textures/tiles.png`, регион `ground` rect {0,0,32,32}, mapping tileId 1 -> ground, emptyId = -1. tile_size читаем из карты (world units), origin top-left, y вниз, render scale 32. Если механики добавят новые tileId/regions, я пришлю полный список; пока этих данных достаточно для доков и сборки. Ресурсов в репо нет — нужно положить tiles.png и (опционально) fonts/debug.ttf рядом с `resources/`.
[Graphics | 2]: Апдейт: в ресурсах теперь есть реальные ассеты. Я грузу tileset из `resources/textures/tileMap.png` (fallback tiles.png) и регистрирую регион `ground` {0,0,32,32}. Debug-шрифт теперь автоматически берётся из `fonts/ArialRegular.ttf` (fallback RobotoMono). Overlay по-прежнему использует alias `debug`, так что отдельный debug.ttf не нужен. Если появятся новые регионы/файлы — пришлю список для доков.

[TL | 3]: Финал для доков: фиксируем `resources/textures/tileMap.png` (fallback tiles.png) с регионом ground {0,0,32,32}, tileId 1 -> ground, emptyId -1, tile_size 1.0 world unit. Шрифт debug: alias `debug` теперь указывает на `resources/fonts/ArialRegular.ttf` (fallback RobotoMono), отдельный debug.ttf не обязателен. Обнови build_run.md и overview_and_map.md этими данными. Если появятся новые регионы — добавим отдельным списком.
